const connection = require('./connection.js');

const orm = () => {
    const selectALL =(table , callBack) => {
        const query = `SELECT * FROM ${table}`;
    };
};
    



module.exports = orm;